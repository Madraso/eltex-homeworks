#include <stdio.h>
#include <stdlib.h>

void input_nums(int *a, int *b) {
    system("clear");
    printf("Введите первое число: ");
    scanf("%d", a);
    printf("Введите второе число: ");
    scanf("%d", b);
}